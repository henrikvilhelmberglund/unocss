// import { splitCodeWithArbitraryVariants } from "@unocss/extractor-arbitrary-variants"
// import { createGenerator, presetUno } from "unocss";

// let myString = "[&_.recharts-cartesian-grid_line[stroke='#ccc']]:text-purple-500"

// let newString = splitCodeWithArbitraryVariants(myString);

// const uno = createGenerator({
//   presets: [
//     presetUno(),
//   ],
// })

// const { css, matched } = await uno.generate(myString, { minify: true, preflights: false })

// console.log(newString)
// console.log(css)
// console.log(matched)
// console.log([...matched].join(' '));
